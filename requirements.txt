# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# HTTP client (MCP calls to Swiggy)
httpx==0.27.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.3.0

# Database
asyncpg==0.29.0

# LLM — Gemini 2.5 Flash for intent parsing
google-generativeai==0.7.2

# Auth — PKCE helpers
cryptography==42.0.8

# Python 3.9 compatibility with Pydantic v2
eval_type_backport==0.2.0

# Utilities
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx  # also used for TestClient
alembic==1.13.1
psycopg2-binary==2.9.9
